import React, { useRef } from "react";

const SearchForm = () => {
    const inputRef = useRef();

    return(
        <div>
            <button></button>
        </div>
    );
}

export default SearchForm;